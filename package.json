{
  "name": "apina-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "concurrently \"cd apps/frontend && npm run dev\" \"cd apps/backend && npm run dev\" --names \"frontend,backend\" --prefix-colors \"cyan,yellow\"",
    "lint": "turbo lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.2",
    "turbo": "latest"
  },
  "packageManager": "pnpm@10.13.1"
}